name: Validate Paper

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  validate:
    uses: Open-Scholar-Nexus/oaktree-sapling-config/.github/workflows/validate-paper.yml@main
